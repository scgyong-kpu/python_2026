# Mid Exam 2026

## 기초 / 변수 / 출력

1. 변수는 값을 저장하는 상자이다. (O)
1. Python에서 `print()`는 값을 출력하는 함수이다. (O)
1. `type()` 함수는 변수의 데이터 타입을 확인할 수 있다. (O)
1. 문자열과 숫자는 서로 다른 타입이다. (O)
1. `"100"`은 문자열이다. (O)
1. `100`은 정수이다. (O)
1. `3.14`는 실수이다. (O)
1. `True`는 불리언 타입이다. (O)
1. 변수는 값을 나중에 바꿀 수 있다. (O)
1. 대입문은 오른쪽 값을 왼쪽 변수에 저장한다. (O)
1. `input()`은 사용자 입력을 받는다. (O)
1. `input()`으로 받은 값은 문자열이다. (O)
1. `int()`는 문자열을 정수로 변환할 수 있다. (O)
1. `float()`는 실수로 변환한다. (O)
1. `str()`은 문자열로 변환한다. (O)
1. `f-string`은 문자열 안에 변수를 넣을 수 있다. (O)
1. `print("a", b)`는 문자열과 변수를 함께 출력할 수 있다. (O)
1. Python은 기본적으로 위에서 아래로 순차 실행된다. (O)
1. 변수 이름은 숫자로 시작할 수 없다. (O)
1. Python은 대소문자를 구분한다. (O)

## 연산자

1. `+`는 덧셈 연산자이다. (O)
1. `-`는 뺄셈 연산자이다. (O)
1. `*`는 곱셈 연산자이다. (O)
1. `/`는 나눗셈 연산자이다. (O)
1. `//`는 몫을 구한다. (O)
1. `%`는 나머지를 구한다. (O)
1. `**`는 거듭제곱이다. (O)
1. `+=`는 값을 더해서 다시 저장한다. (O)
1. `-=`도 복합 대입 연산자이다. (O)
1. `==`는 같은지 비교한다. (O)
1. `!=`는 같지 않음을 비교한다. (O)
1. `>`는 크다를 의미한다. (O)
1. `<`는 작다를 의미한다. (O)
1. `>=`는 크거나 같다를 의미한다. (O)
1. `<=`는 작거나 같다를 의미한다. (O)
1. 비교 연산의 결과는 `bool`이다. (O)
1. `and`는 둘 다 참일 때 참이다. (O)
1. `or`는 하나라도 참이면 참이다. (O)
1. 문자열은 `+`로 이어붙일 수 있다. (O)
1. 문자열은 `*`로 반복할 수 있다. (O)

## 조건문

1. `if`는 조건이 참일 때 실행된다. (O)
1. 조건식의 결과는 `True` 또는 `False`이다. (O)
1. `if` 뒤에는 `:`가 필요하다. (O)
1. 들여쓰기는 코드 블록을 구분한다. (O)
1. `if-else`는 두 가지 경우를 처리한다. (O)
1. `elif`는 여러 조건을 처리할 수 있다. (O)
1. 조건문은 위에서부터 순서대로 검사한다. (O)
1. `if` 안에 또 `if`를 넣을 수 있다. (O)
1. 이를 중첩 `if`라고 한다. (O)
1. `if x in [1, 2, 3]`은 포함 여부를 검사한다. (O)
1. `not`은 논리를 반대로 만든다. (O)
1. 조건문은 프로그램의 흐름을 바꾼다. (O)
1. 조건문은 비교 연산자와 함께 사용된다. (O)
1. 조건문은 논리 연산자와 함께 사용된다. (O)
1. `if x > 0:`은 `x`가 양수일 때 실행된다. (O)
1. `else`는 조건이 거짓일 때 실행된다. (O)
1. `elif`는 중간 조건을 추가할 때 사용한다. (O)
1. 조건문 없이도 간단한 프로그램은 만들 수 있다. (O)
1. 조건문은 선택 구조이다. (O)
1. 조건문은 하나의 흐름을 여러 갈래로 나눈다. (O)

## 반복문

1. `for`는 반복문이다. (O)
1. `range()`는 반복 범위를 만든다. (O)
1. `range(5)`는 `0`부터 `4`까지 총 `5`개의 값을 만든다. (O)
1. 반복문은 같은 작업을 여러 번 수행한다. (O)
1. 반복문은 자동화를 가능하게 한다. (O)
1. 반복문에서는 반복마다 값이 바뀌는 변수를 자주 사용한다. (O)
1. `for` 안에 `if`를 사용할 수 있다. (O)
1. 반복문 안에서 조건을 검사할 수 있다. (O)
1. 반복문은 누적 계산에 사용된다. (O)
1. `sum += i`는 값을 누적한다. (O)
1. 반복문 안에 반복문을 넣을 수 있다. (O)
1. 이를 중첩 반복문이라고 한다. (O)
1. `while`도 반복문이다. (O)
1. `break`는 반복을 종료한다. (O)
1. `continue`는 다음 반복으로 넘어간다. (O)
1. 반복문 안에서 조건문을 사용할 수 있다. (O)
1. 반복문은 패턴을 만드는 데 사용된다. (O)
1. 반복문은 여러 데이터를 처리할 때 자주 사용된다. (O)
1. 반복문은 알고리즘의 핵심 요소 중 하나이다. (O)
1. 반복문은 코드 길이를 줄여준다. (O)

## 종합 / `turtle` / 사고

1. `turtle`은 그래픽을 그릴 수 있는 모듈이다. (O)
1. `forward()`는 앞으로 이동한다. (O)
1. `left()`는 방향을 회전한다. (O)
1. 반복문으로 도형을 그릴 수 있다. (O)
1. 각도를 바꾸면 도형이 달라진다. (O)
1. 조건문으로 색을 바꿀 수 있다. (O)
1. 프로그램은 입력 → 처리 → 출력 구조를 가진다. (O)
1. 연산자는 값을 계산하는 데 사용된다. (O)
1. 조건문은 판단을 수행한다. (O)
1. 반복문은 작업을 반복한다. (O)
1. 프로그램은 여러 개념이 함께 사용되어 동작할 수 있다. (O)
1. 알고리즘은 문제를 해결하는 방법이다. (O)
1. 변수는 상태를 저장한다. (O)
1. 조건문은 상태에 따라 다르게 동작할 수 있다. (O)
1. 반복문은 상태를 변화시키는 데 자주 사용된다. (O)
1. Python은 들여쓰기가 중요한 언어이다. (O)
1. 코드의 순서는 결과에 영향을 준다. (O)
1. 잘못된 타입 연산은 오류를 발생시킬 수 있다. (O)
1. 프로그래밍은 논리적인 사고를 필요로 한다. (O)
1. 문제를 작은 단계로 나누면 해결하기 쉽다. (O)


