# 기말고사 대비 정리

## 함수 기초

1. 함수는 `def` 키워드로 정의할 수 있다. (O)
1. 함수는 필요할 때 여러 번 호출할 수 있다. (O)
1. 함수의 매개변수는 함수에 값을 전달받기 위해 사용한다. (O)
1. 함수는 `return`을 이용해 값을 돌려줄 수 있다. (O)
1. `return`이 없는 함수는 기본적으로 `None`을 반환한다. (O)
1. 함수 안에서는 `if` 문을 사용할 수 있다. (O)
1. 함수 안에서는 `for` 문을 사용할 수 있다. (O)
1. 같은 프로그램 안에 여러 개의 함수를 정의할 수 있다. (O)
1. 함수는 반복되는 코드를 묶어 재사용하기 좋게 만든다. (O)
1. 함수 호출 시 인자의 순서는 결과에 영향을 줄 수 있다. (O)

## 함수 심화 / 변수 범위

1. 함수 안에서 만든 변수는 일반적으로 지역변수이다. (O)
1. 지역변수는 그 함수 바깥에서 직접 사용할 수 없다. (O)
1. 함수 밖에서 만든 변수는 전역변수라고 부를 수 있다. (O)
1. 함수 안에서 전역변수를 읽을 수 있다. (O)
1. 같은 이름의 변수가 함수 안과 밖에 동시에 있을 수 있다. (O)
1. 함수는 다른 함수를 호출할 수 있다. (O)
1. 반환값을 변수에 저장해서 나중에 사용할 수 있다. (O)
1. `return` 문이 실행되면 함수는 그 자리에서 끝난다. (O)
1. 매개변수가 없는 함수도 만들 수 있다. (O)
1. 매개변수가 여러 개인 함수도 만들 수 있다. (O)

## 리스트 / 튜플

1. 리스트는 여러 값을 순서대로 저장할 수 있다. (O)
1. 리스트는 원소를 추가하거나 수정할 수 있다. (O)
1. `append()`는 리스트의 맨 뒤에 원소를 추가한다. (O)
1. `pop()`은 리스트에서 원소를 꺼내면서 제거할 수 있다. (O)
1. `remove()`는 값으로 리스트의 원소를 삭제할 수 있다. (O)
1. 튜플은 소괄호 `()`를 사용해 만들 수 있다. (O)
1. 튜플은 리스트와 달리 보통 내용을 수정하지 않는 용도로 사용한다. (O)
1. 리스트는 인덱스를 이용해 원소에 접근할 수 있다. (O)
1. 슬라이싱은 리스트의 일부 구간을 잘라서 얻는 방법이다. (O)
1. `len()` 함수는 리스트나 튜플의 길이를 구할 수 있다. (O)

## 딕셔너리 / 집합

1. 딕셔너리는 key와 value의 쌍으로 데이터를 저장한다. (O)
1. 딕셔너리의 key를 이용해 value를 찾을 수 있다. (O)
1. 딕셔너리에는 새로운 key-value 쌍을 추가할 수 있다. (O)
1. 이미 있는 key에 값을 다시 대입하면 value가 바뀐다. (O)
1. `keys()`는 딕셔너리의 key들을 확인할 때 사용할 수 있다. (O)
1. `values()`는 딕셔너리의 value들을 확인할 때 사용할 수 있다. (O)
1. `items()`는 key와 value를 함께 확인할 때 사용할 수 있다. (O)
1. 집합(`set`)은 중복 없는 값을 저장하는 데 적합하다. (O)
1. 집합은 `add()`로 원소를 추가할 수 있다. (O)
1. 집합은 합집합, 교집합, 차집합 같은 연산에 사용할 수 있다. (O)

## 파일 입출력

1. `open()` 함수로 파일을 열 수 있다. (O)
1. 파일을 읽기 전용으로 열 때는 보통 `"r"` 모드를 사용한다. (O)
1. 파일을 쓰기 전용으로 열 때는 보통 `"w"` 모드를 사용한다. (O)
1. 파일에 이어서 쓰고 싶을 때는 보통 `"a"` 모드를 사용한다. (O)
1. `read()`는 파일 전체 내용을 한 번에 읽을 수 있다. (O)
1. `readline()`은 파일에서 한 줄을 읽을 수 있다. (O)
1. 파일 객체를 반복문으로 순회하면 줄 단위로 읽을 수 있다. (O)
1. `with open(...) as f:` 형태를 사용하면 파일 닫기를 자동으로 처리할 수 있다. (O)
1. 파일에 문자열을 저장할 때 줄바꿈이 필요하면 `\n`을 사용할 수 있다. (O)
1. 같은 파일이라도 읽기 모드와 쓰기 모드는 목적이 다르다. (O)

## 예외 처리

1. Python에서는 실행 중 오류가 발생할 수 있다. (O)
1. `try` 문은 오류가 발생할 수 있는 코드를 감싸는 데 사용한다. (O)
1. `except` 문은 예외가 발생했을 때 처리할 코드를 작성하는 데 사용한다. (O)
1. `ValueError`는 값의 형식이 잘못되었을 때 발생할 수 있다. (O)
1. 문자열을 `int()`로 바꾸는 과정에서 실패하면 `ValueError`가 발생할 수 있다. (O)
1. 예외 처리를 하면 프로그램이 바로 종료되지 않고 다음 동작을 이어갈 수 있는 경우가 있다. (O)
1. `raise`를 사용하면 일부러 예외를 발생시킬 수 있다. (O)
1. 예외 처리는 파일 읽기나 사용자 입력 처리에서 유용하다. (O)
1. `try-except`는 잘못된 입력이 들어와도 프로그램을 계속 동작하게 만드는 데 도움이 된다. (O)
1. 예외가 발생했을 때 메시지를 출력하고 다음 줄로 넘어가도록 만들 수 있다. (O)

## 정규식 / 문자열 처리

1. `import re`를 하면 정규식 기능을 사용할 수 있다. (O)
1. `re.match()`는 문자열의 시작 부분이 패턴과 맞는지 검사할 수 있다. (O)
1. 정규식에서 `\d`는 숫자를 의미한다. (O)
1. 정규식에서 `\s`는 공백 문자를 의미한다. (O)
1. 정규식에서 `+`는 앞의 패턴이 한 번 이상 반복됨을 나타낼 수 있다. (O)
1. 정규식의 괄호 `()`는 그룹을 만들 수 있다. (O)
1. `match.group(1)`처럼 그룹으로 묶인 부분을 꺼낼 수 있다. (O)
1. 문자열의 `split()`은 구분자를 기준으로 나누는 데 사용할 수 있다. (O)
1. `" ".join(list)`는 문자열 리스트를 하나의 문자열로 합칠 수 있다. (O)
1. `strip()`은 문자열 양쪽 끝의 공백이나 줄바꿈을 제거할 때 유용하다. (O)

## 객체지향 기초

1. 클래스는 객체를 만들기 위한 설계도라고 볼 수 있다. (O)
1. 객체는 클래스로부터 만들어진다. (O)
1. `class` 키워드로 클래스를 정의할 수 있다. (O)
1. `__init__()`은 객체가 생성될 때 호출되는 초기화 메서드이다. (O)
1. `self`는 현재 객체 자신을 가리키는 데 사용한다. (O)
1. 객체의 속성은 `self.name` 같은 형태로 저장할 수 있다. (O)
1. 클래스 안에 함수를 정의하면 메서드가 된다. (O)
1. 메서드는 객체의 상태를 읽거나 바꾸는 데 사용할 수 있다. (O)
1. 하나의 클래스로 여러 개의 객체를 만들 수 있다. (O)
1. 객체를 리스트에 저장해서 관리할 수 있다. (O)

## 객체지향 응용 / 표현 / 파싱

1. 생성자에서 문자열 한 줄을 받아 객체의 속성으로 나누어 저장할 수 있다. (O)
1. 객체 생성 중 잘못된 형식의 데이터가 들어오면 `ValueError`를 발생시키도록 만들 수 있다. (O)
1. `__repr__()`을 정의하면 객체를 문자열 형태로 보기 좋게 표현할 수 있다. (O)
1. 객체의 속성에 `int`, `float`, `str` 타입을 구분해서 저장할 수 있다. (O)
1. 파일에서 한 줄씩 읽어 객체로 만드는 프로그램을 작성할 수 있다. (O)
1. 잘못된 줄은 건너뛰고 정상적인 줄만 객체로 저장할 수 있다. (O)
1. 객체의 메서드 안에서 formatted string을 사용해 출력 형식을 맞출 수 있다. (O)
1. 사용자의 숫자 입력을 받아 리스트에 저장된 객체를 찾아 출력할 수 있다. (O)
1. 리스트의 인덱스와 실제 데이터의 순번은 다를 수 있으므로 주의가 필요하다. (O)
1. 파일 데이터의 형식이 예상과 다르면 예외 처리로 대응할 수 있다. (O)

## SQLite3

1. `sqlite3` 모듈을 이용하면 Python에서 SQLite 데이터베이스를 사용할 수 있다. (O)
1. SQLite 데이터베이스는 파일 형태로 저장될 수 있다. (O)
1. `sqlite3.connect()`로 데이터베이스에 연결할 수 있다. (O)
1. 연결 객체에서 `cursor()`를 만들어 SQL을 실행할 수 있다. (O)
1. `execute()`로 SQL 문장을 실행할 수 있다. (O)
1. `CREATE TABLE`은 테이블을 만드는 SQL 문이다. (O)
1. `INSERT INTO`는 데이터를 추가하는 SQL 문이다. (O)
1. `SELECT`는 데이터를 조회하는 SQL 문이다. (O)
1. `conn.commit()`은 변경 내용을 저장하는 데 사용한다. (O)
1. `conn.close()`는 데이터베이스 연결을 닫는 데 사용한다. (O)
